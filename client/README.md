# Dating App Client

This is the frontend for the Dating App using React.

## Setup

1. Install dependencies:
   ```sh
   cd client
   npm install
   ```
2. Start the development server:
   ```sh
   npm start
   ```

The app will run on http://localhost:3000 by default.
